attributes:

  node_name:
    value: 'influxdb'
    label: 'Node name'
    description: 'The name of the node where the plugin will be deployed'
    weight: 10
    type: "text"
    regex:
      source: '\S'
      error: "You must provide a name"

  dedicated_disks:
    value: ''
    label: 'Dedicated disks'
    description: 'Comma-separated list of disk devices used to store InfluxDB data (for instance "sda,sdb"). Keep it empty means using "/"'
    weight: 20
    type: "text"

  influxdb_rootpass:
    value: ''
    label: 'root password'
    description: 'Password of InfluxDB root'
    weight: 30
    type: "password"
    regex:
      source: '\S'
      error: "You must provide a password"

  influxdb_dbname:
    value: 'lma'
    label: 'db name'
    description: 'Name of the database used to store metrics'
    weight: 40
    type: "text"
    regex:
      source: '\S'
      error: "You must provide a name"

  influxdb_username:
    value: 'lma'
    label: 'user name'
    description: 'Name of InfluxDB user'
    weight: 50
    type: "text"
    regex:
      source: '\S'
      error: "You must provide a username"

  influxdb_userpass:
    value: ''
    label: 'user password'
    description: 'Password of InfluxDB user'
    weight: 60
    type: "password"
    regex:
      source: '\S'
      error: "You must provide a password"
